class ManyStatements {
    public static void main() {
        int a = 0;
        #*; // the list wildcard is redundant with non-strict mode, but still good to test it just in case
        System.out.println(a);
    }
}